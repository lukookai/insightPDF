# 迁移说明

本包为「DeepSeek 三方案 PDF 翻译对比」迁移包，包含两部分内容。

## 1. 结果与交付物：`DeepSeek三方案对比_20260802/`

两篇论文（`2404.16130v1`、`2504.05732v2`）× 三个方案（当前 DocLayout+HTML、生产规则兼容、double6 Skill）的：

- **输入源文件**：原始 PDF（论文根目录）、`source.md`、源清单
- **主要交付物**：译文 JSON、`translation.md`、输出 PDF、HTML、QA 清单
- 已剔除 BabelDOC 中间调试文件（`work/`、`production_work/`），原目录共 644 MB，本部分约 94 MB

## 2. 实现源码：`实现源码/`

| 目录 | 对应方案 | 说明 |
|---|---|---|
| `pdf_translator/` | 方案1 当前 HTML 版 | 主应用，`run-babeldoc-html` 翻译管线 |
| `image_structure_json_repo/` | 方案1 排版引擎 | DocLayout 识别 + 中间结构转 HTML |
| `pdf2json_repo/` | 方案2 生产规则版 | 生产原生解析规则 + 坐标回填 |
| `babeldoc/` | 方案3 Skill 版 | BabelDOC 原生排版库（0.6.2） |
| `pdf2zh_next/` | 方案3 Skill 版 | pdf2zh_next 翻译库（2.9.0） |

> 说明：`.double6-python` 是编译好的 CPython 运行时（非源码），未打包；`.double6-runtime` 中除 `babeldoc`/`pdf2zh_next` 之外的第三方依赖也未打包。目标机器需先安装 Python 依赖再运行。

## 3. 迁移后注意事项

- 各方案 manifest / 清单中的绝对路径（如 `source_pdf: /data/qhy/cqnu/...`）仍指向原机器，迁移后需按新环境修改。
- 运行入口与命令示例见原机器 `/data/qhy/cqnu/PDF_TRANSLATOR_README.md` 及 `docs/BABELDOC_HTML_ARCHITECTURE.md`。
- 迁移包仅包含源码与交付物，BabelDOC 中间缓存（`work/`）不随包迁移，首次运行会重新生成。
