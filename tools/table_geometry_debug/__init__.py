"""Independent PDF table-geometry diagnostic tool.

Does NOT modify the translation pipeline.  Two parsing paths on one page:
  * Path A -- BabelDOC native IL (no translation / no DeepSeek)
  * Path B -- raw PDF geometry via PyMuPDF (project dependency)
"""
